<div class="absolute inset-0 flex flex-col">
  <div class="flex-1">
    <h2 class="text-4xl">Choose your level</h2>
    <p class="mb-4">難易度を選んでください</p>

    <div class="flex text-xs mb-4 justify-center">
      <button {{on "click" (fn this.selectLevelType 1)}} class="border w-40 h-32 mx-2 hover:bg-gray-100 hover:border-2 text-left p-2 {{if (eq this.levelType 1) "bg-gray-100 border-2"}}">
        全員同じ<br>
        卜リツクレベルで<br>
        ス夕一卜
      </button>

      <button {{on "click" (fn this.selectLevelType 2)}} class="border w-40 h-32 mx-2 hover:bg-gray-100 hover:border-2 text-left p-2 {{if (eq this.levelType 2) "bg-gray-100 border-2"}}">
        1人1人レベルを<br>
        選択する
      </button>

      <button {{on "click" (fn this.selectLevelType 3)}} class="border w-40 h-32 mx-2 hover:bg-gray-100 hover:border-2 text-left p-2 {{if (eq this.levelType 3) "bg-gray-100 border-2"}}">
        オ一卜ハンデ…<br>
        DENDAMA力一<br>
        ドのレベルに応じ<br>
        て自動決定(力一 ドを<br>
        挿入して〈ださい)
      </button>
    </div>

    {{!--
    <div class="flex items-center justify-center">
      <button {{on "click" this.prevLevelType}}>
        <svg version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px" viewBox="0 0 12 24" width="12" height="24" xml:space="preserve">
          <path d="M12.1,0l-12,12l12,12V0z"/>
        </svg>
      </button>

      <button {{on "click" this.select}} class="bg-green-500 text-white py-1 px-6 mx-2 text-xl">SELECT</button>

      <button {{on "click" this.nextLevelType}}>
        <svg version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px" viewBox="0 0 12 24" width="12" height="24" xml:space="preserve">
          <path d="M0,24l12-12L0,0V24z" />
        </svg>
      </button>
    </div>
    --}}
  </div>

  <div class="flex justify-between items-end px-4">
    {{#each players as |player|}}
      <span class="border border-b-0 flex-1 mx-1">PLAYER{{player}}</span>
    {{/each}}
  </div>
</div>