import Controller from '@ember/controller';
// import { tracked } from '@glimmer/tracking';
// import { action } from '@ember/object';
// import { inject as service } from '@ember/service';

export default class ChooseLevelController extends Controller {
  // @service game;
  // @tracked gameType = this.game.gameType;
  // @tracked currentPlayer = 1;
  // @tracked showLevelPicker = false;

  // get players() {
  //   return Array.from({ length: this.game.playersCount }, (v, i) => i + 1);
  // }

  // @action selectGameType(type) {
  //   this.gameType = type;
  // }

  // @action select() {
  //   console.log(`selected, go to the next route with type ${this.gameType}`);
  //   this.game.gameType = this.gameType;
  //   this.router.transitionTo('choose-level');
  // }
}
